<%= form_for(study_cycle) do |f| %>
  <% if study_cycle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(study_cycle.errors.count, "error") %> prohibited this study_cycle from being saved:</h2>

      <ul>
      <% study_cycle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row">
    <div class="field espaciado">
      <%= f.label :Inicio_de_ciclo, class:"colorTextoCuatro" %>
      <%= f.date_select :start_at %>
    </div>

    <div class="field espaciado">
      <%= f.label :Fin_de_ciclo, class:"colorTextoCuatro" %>
      <%= f.date_select :end_at %>
    </div>
  </div>

  <div class="row">
    <div class="actions espaciado">
      <%= f.submit 'Crear Ciclo escolar', class:"btn colorBackgroundTres colorTextoCinco"%>
    </div>
  </div>

<% end %>
